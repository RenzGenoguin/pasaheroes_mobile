import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Account = () => {
    return ( <SafeAreaView><Text>Account</Text></SafeAreaView> );
}
 
export default Account;